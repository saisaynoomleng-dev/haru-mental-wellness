import Bounded from '@/components/Bounded';
import { Button } from '@/components/ui/button';

export default function Home() {
  return (
    <Bounded>
      Welcome Next.js <Button className="">Hi</Button>
    </Bounded>
  );
}
